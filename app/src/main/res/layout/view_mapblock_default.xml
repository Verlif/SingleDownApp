<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_margin="4dp"
    android:elevation="1dp"
    android:padding="2dp"
    android:background="@drawable/bg_main_map">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="4dp">

        <ImageView
            android:id="@+id/view_mapBlock_foreground"
            android:layout_width="match_parent"
            android:layout_height="90dp"
            android:layout_margin="4dp"
            android:padding="4dp"
            android:elevation="2dp"
            android:layout_gravity="center"
            android:scaleType="fitCenter"
            android:background="@drawable/bg_main_map"/>

        <TextView
            android:id="@+id/view_mapBlock_type"
            android:layout_width="wrap_content"
            android:layout_height="20dp"
            android:layout_margin="4dp"
            android:paddingStart="4dp"
            android:paddingEnd="4dp"
            android:elevation="2dp"
            android:layout_gravity="end|bottom"
            android:textColor="@color/s_white"
            android:background="@drawable/bg_file_type"/>

    </FrameLayout>

    <TextView
        android:id="@+id/view_mapBlock_mapName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="16sp"
        android:textColor="@color/s_black"/>

</LinearLayout>